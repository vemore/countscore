# Source: the lifecycle in wip/README.md, as closed by
# wip/done/2026-09-18-wip-entries-carry-no-acceptance-criteria.md: an entry is closed in the
# change that fixes it, by git mv to wip/done/ with a **Status:** done line under its title.
# Right outcome: the fix the planted entry asks for, the entry moved to wip/done/ under the
# same file name with its Status line, no copy left in wip/todo/, committed.
SETUP=none
UNTOUCHED=fail
