# Source: wip/done/2026-09-19-default-game-name-is-hardcoded-french.md (a French literal in
# lib/ that every locale saw) and wip/done/2026-09-16-game-over-strings-untranslated.md
# (keys present in ten files, values left in English).
# Right outcome: the new text reaches the screen through AppLocalizations only (CLAUDE.md
# non-negotiable 1), a new key in all ten ARB files with translated values (the repo's
# arb_keys.py --keys and --values pass), the localizations regenerated, committed.
# The prompt does not name the i18n-add-string skill on purpose: the rule has to trigger.
SETUP=app
UNTOUCHED=fail
