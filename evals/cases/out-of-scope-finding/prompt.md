Users say the Undo snackbar disappears before they can reach it. Keep Undo on offer for
8 seconds instead of 6 (`lib/utils/undo_snack_bar.dart`). That is the whole task; anything
else you notice along the way, handle the way this project handles it.
